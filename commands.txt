# In order to bundle my js and use require in the browser ;) #
browserify index.js -o bundle.js 

# Watcher in order to detect changes 
watchify index.js -o bundle.js

# Build-Bundle-Minify

browserify index.js | uglifyjs > bundle.min.js


# Installed Babelify in order to enable import and export of the modules  with ES2015 syntax # 

